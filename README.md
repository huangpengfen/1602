系统开机处于正常读取环境温湿度同时正常显示温湿度的状态；当温湿度超过系统设置的阈值时，系统报警（蜂鸣器响，继电器打上）。
1、模式设置（系统共有3个按键）
第1个按键是模式选择；
当按下按键1时，进入设置温湿度阈值模式：
第一次开始设置的是湿度的高值（此时可以操作按键1跟按键2，按键1是将湿度高值加1，按键2是将湿度高值减1），
第二次设置的是湿度的低值（按键1,2作用同上），
第三次置的是温度的高值（按键1,2作用同上），
第四次置的是温度的低值（按键1,2作用同上）。
当按下按键1第五次时，系统将上面四次修改后的阈值写入at24c02中，并恢复系统初始状态。